@model IEnumerable<Data.ViewModels.ApplicationUserViewModel>
@{
    ViewData["Title"] = "Researchers";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<style>

	body {
		counter-reset: Serial; /* / Set the Serial counter to 0 /*/
	}

	tr td:first-child:before {
		counter-increment: Serial; /* / Increment the Serial counter /*/
		content: counter(Serial); /*  / Display the counter /*/
	}
</style>

<!-- Page Wrapper -->
<div class="page-wrapper">
	<div class="content">

		<!-- Breadcrumb -->
		<div class="d-md-flex d-block align-items-center justify-content-between page-breadcrumb mb-3">
			<div class="my-auto mb-2">
				<h2 class="mb-1">Researchers</h2>
			</div>
			<div class="d-flex my-xl-auto right-content align-items-center flex-wrap ">
				<div class="mb-2">
					<a href="#" data-bs-toggle="modal" data-bs-target="#add_User" class="btn btn-primary d-flex align-items-center"><i class="ti ti-circle-plus me-2"></i>Register</a>
				</div>
			</div>
		</div>
		<!-- /Breadcrumb -->
		
		<!-- Clients list -->
		<div class="card">
			<div class="card-body p-0">
				<div class="custom-datatable-filter table-responsive">
					<table class="table datatable">
						<thead class="thead-light">
							<tr>
								<th>S/n</th>
								<th>Full Name</th>
								<th>Email</th>
								<th>Phone Number</th>
								<th>Access Duration</th>
								<th></th>
							</tr>
						</thead>
						<tbody>
							@if (Model != null && Model.Count() > 0)
							{
								@foreach (var applicationUser in Model)
								{
									<tr>
										<td></td>
										<td>
											@applicationUser.FullName
											@if (applicationUser.IsAdmin)
											{
												<span class="badge bg-primary ms-2" style="font-size: 0.7rem;">Admin</span>
											}
										</td>
										<td>@applicationUser.Email</td>
										<td>@applicationUser.PhoneNumber</td>
										<td>
											@applicationUser.PassWordTypeDescription

											@if (applicationUser.IsExtended)
											{
												<span class="badge bg-primary ms-2" style="font-size: 0.7rem;">Extended</span>
											}

											@if (applicationUser.IsPasswordExpired)
											{
												<span class="badge bg-danger ms-2" style="font-size: 0.7rem;">Expired</span>
											}
											@if (applicationUser.PasswordExpiryDate.HasValue && applicationUser.PasswordExpiryDate != DateTime.MaxValue)
											{
												var daysLeft = (applicationUser.PasswordExpiryDate.Value - DateTime.Now).Days;
												if (daysLeft > 0)
												{
													<span class="badge bg-success ms-2" style="font-size: 0.7rem;">@daysLeft days left</span>
												}
											}
										</td>
										<td>
											<div class="action-icon d-inline-flex">
												<a href="javascript:void(0);" onclick="openMakeAdminModal('@applicationUser.Id')" title="Make Admin">
													<i class="ti ti-shield-check"></i>
												</a>
												<a href="javascript:void(0);" onclick="openChangeTypeModal('@applicationUser.Id')" title="Change Duration"><i class="ti ti-clock-edit"></i></a>
												<a href="javascript:void(0);" onclick="openExtendDurationModal('@applicationUser.Id')" title="Extend Duration"><i class="ti ti-calendar-plus"></i></a>
												<a href="javascript:void(0);" onclick="openChangePasswordModal('@applicationUser.Id')" title="Change Password"><i class="ti ti-lock-open"></i></a>
												<a href="javascript:void(0);" data-bs-toggle="modal" data-bs-target="#delete_modal" title="Delete"><i class="ti ti-trash"></i></a>
											</div>

										</td>

									</tr>
								}
							}
						</tbody>
					</table>
				</div>
			</div>
		</div>
		<!-- /Clients list -->
	</div>
</div>
<!-- /Page Wrapper -->
<!-- Add Client -->
<div class="modal fade" id="add_User">
	<div class="modal-dialog modal-dialog-centered modal-md">
		<div class="modal-content">
			<div class="modal-header">
				<h4 class="modal-title">Register User</h4>
				<button type="button" class="btn-close custom-btn-close" data-bs-dismiss="modal" aria-label="Close">
					<i class="ti ti-x"></i>
				</button>
			</div>
			<div class="modal-body pb-0 ">
				<div class="row">
					<div class="col-md-12">
						<div class="mb-3">
							<label class="form-label">Full Name <span class="text-danger"> *</span></label>
							<input type="text" class="form-control" id="fullName" />
						</div>
					</div>

					<div class="col-md-12">
						<div class="mb-3">
							<label class="form-label">Email<span class="text-danger"> *</span></label>
							<input type="text" class="form-control" id="email" />
						</div>
					</div>
					<div class="col-md-12">
						<div class="mb-3">
							<label class="form-label">Phone Number</label>
							<input type="text" class="form-control" id="phone" />
						</div>
					</div>
					<div class="col-md-12">
						<div class="mb-3 ">
							<label class="form-label">Log In Duration <span class="text-danger"> *</span></label>
							<select class="select" id="duration">
								<option>Select</option>
								<option>Two Weeks</option>
								<option>One Week</option>
								<option>Never</option>
							</select>
						</div>
					</div>
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-outline-light border me-2" data-bs-dismiss="modal">Cancel</button>
				<button type="submit" onclick="registerUser()" id="submit_btn" class="btn btn-primary">Save </button>
			</div>
		</div>
	</div>
</div>
<!-- /Add Client -->
<!-- Delete Modal -->
<div class="modal fade" id="delete_modal">
	<div class="modal-dialog modal-dialog-centered">
		<div class="modal-content">
			<div class="modal-body text-center">
				<span class="avatar avatar-xl bg-transparent-danger text-danger mb-3">
					<i class="ti ti-trash-x fs-36"></i>
				</span>
				<h4 class="mb-1">Confirm Delete</h4>
				<p class="mb-3">You want to delete all the marked items, this cant be undone once you delete.</p>
				<div class="d-flex justify-content-center">
					<a href="javascript:void(0);" class="btn btn-light me-3" data-bs-dismiss="modal">Cancel</a>
					<a href="#" class="btn btn-danger">Yes, Delete</a>
				</div>
			</div>
		</div>
	</div>
</div>
<!-- /Delete Modal -->
<!-- Change Password Type Modal -->
<div class="modal fade" id="change_password_type_modal">
	<div class="modal-dialog modal-dialog-centered modal-md">
		<div class="modal-content">
			<div class="modal-header">
				<h4 class="modal-title">Change Password Type</h4>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
					<i class="ti ti-x"></i>
				</button>
			</div>
			<div class="modal-body">
				<input type="hidden" id="userIdForType" />
				<div class="mb-3">
					<label class="form-label">Select New Duration</label>
					<select class="select" id="newPassType">
						<option value="">Select</option>
						<option value="TwoWeeks">Two Weeks</option>
						<option value="OneWeek">One Week</option>
						<option value="DoNotExpire">Do Not Expire</option>
					</select>
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-outline-light border me-2" data-bs-dismiss="modal">Cancel</button>
				<button type="button" id="change_type_btn" class="btn btn-primary" onclick="changePasswordType()">Save</button>
			</div>
		</div>
	</div>
</div>

<!-- Change Password Modal -->
<div class="modal fade" id="change_password_modal">
	<div class="modal-dialog modal-dialog-centered modal-md">
		<div class="modal-content">
			<div class="modal-header">
				<h4 class="modal-title">Change Password</h4>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
					<i class="ti ti-x"></i>
				</button>
			</div>
			<div class="modal-body">
				<input type="hidden" id="userIdForPassword" />
				<div class="mb-3">
					<label class="form-label">New Password</label>
					<input type="password" class="form-control" id="newPassword" placeholder="Enter new password" />
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-outline-light border me-2" data-bs-dismiss="modal">Cancel</button>
				<button type="button" id="change_pass_btn" class="btn btn-primary" onclick="changePassword()">Change</button>
			</div>
		</div>
	</div>
</div>
<!-- Extend Password Duration Modal -->
<div class="modal fade" id="extend_duration_modal">
	<div class="modal-dialog modal-dialog-centered modal-md">
		<div class="modal-content">
			<div class="modal-header">
				<h4 class="modal-title">Extend Password Duration</h4>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
					<i class="ti ti-x"></i>
				</button>
			</div>
			<div class="modal-body">
				<input type="hidden" id="userIdForExtension" />
				<div class="mb-3">
					<label class="form-label">Add Days</label>
					<input type="number" id="extraDays" class="form-control" placeholder="Enter number of days to add"/>
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-outline-light border me-2" data-bs-dismiss="modal">Cancel</button>
				<button type="button" class="btn btn-primary" onclick="extendPasswordDuration()">Extend</button>
			</div>
		</div>
	</div>
</div>
<!-- Make admin Modal -->
<div class="modal fade" id="makeAdmin_modal">
	<div class="modal-dialog modal-dialog-centered">
		<div class="modal-content">
			<div class="modal-body text-center">
				<span class="avatar avatar-xl bg-transparent-warning text-warning mb-3">
					<i class="ti ti-shield-check fs-36"></i>
				</span>
				<h4 class="mb-1">Make Admin</h4>
				<input type="hidden" id="userIdToMakeId" />
				<p class="mb-3">Are you sure you want to make this user an admin</p>
				<div class="d-flex justify-content-center">
					<a href="javascript:void(0);" class="btn btn-light me-3" data-bs-dismiss="modal">Cancel</a>
					<a onclick="makeUserAdmin()" class="btn btn-warning">Proceed</a>
				</div>
			</div>
		</div>
	</div>
</div>
<!-- /Make admin Modal -->